steps:
  - name: python:3.10
    entrypoint: bash
    args:
      - -c
      - |
        pip install -r requirements.txt
        python agent.py --dry_run
        
options:
  
  logging: CLOUD_LOGGING_ONLY
